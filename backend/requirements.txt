# ⚡ Use --prefer-binary to avoid compiling from source (much faster on Render)
# FastAPI & Server
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Auth & Security
python-jose[cryptography]==3.3.0
bcrypt==4.1.3

# Database
sqlalchemy==2.0.30
aiosqlite==0.20.0

# AI / LLM
groq==0.9.0
# sentence-transformers, scikit-learn, numpy removed — PyTorch alone uses ~350MB
# which exceeds Render free tier's 512MB RAM limit.
# The app automatically falls back to fuzzy string matching (see skill_matcher.py).

# Resume Parsing
pypdf2==3.0.1
python-docx==1.1.2
pdfminer.six==20231228

# PDF Export
reportlab==4.2.2

# Utils
python-dotenv==1.0.1
pydantic==2.7.3
pydantic-settings==2.3.1
httpx==0.27.0
aiofiles==23.2.1
